# Development

First, run the development server:

```bash
npm run dev

npm run build
firebase serve --only hosting
```

# Deploy

First, run the development server:

```bash
npm run build
firebase deploy
```


